(function(){
  if(window.KP_V29_STABLE_GITHUB_SYNC)return;
  window.KP_V29_STABLE_GITHUB_SYNC=true;
  const V='v29.5';
  let running=false;
  const toast=m=>{try{typeof showToast==='function'?showToast(m):console.log(m)}catch(e){}};
  const log=m=>{try{typeof githubLog==='function'?githubLog(m):console.log('[KapásPont '+V+'] '+m)}catch(e){}};
  const pad=n=>String(n).padStart(2,'0');
  const stamp=()=>{const d=new Date();return d.getFullYear()+'-'+pad(d.getMonth()+1)+'-'+pad(d.getDate())+'T'+pad(d.getHours())+'-'+pad(d.getMinutes())+'-'+pad(d.getSeconds())};
  const slug=s=>String(s||'adat').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9._-]+/g,'-').replace(/^-+|-+$/g,'').slice(0,90)||'adat';
  const enc=s=>{s=String(s||'');try{return btoa(unescape(encodeURIComponent(s)))}catch(e){const b=new TextEncoder().encode(s);let bin='';for(let i=0;i<b.length;i++)bin+=String.fromCharCode(b[i]);return btoa(bin)}};
  const dec=s=>{try{return decodeURIComponent(escape(atob(String(s||'').replace(/\n/g,''))))}catch(e){try{return new TextDecoder().decode(Uint8Array.from(atob(String(s||'').replace(/\n/g,'')),c=>c.charCodeAt(0)))}catch(_){return ''}}};
  const ib64=d=>(String(d||'').match(/^data:[^;]+;base64,(.+)$/)||[])[1]||'';
  const ext=d=>{const m=String(d||'').match(/^data:([^;]+);base64,/);return m?((m[1].split('/')[1]||'jpg').replace('jpeg','jpg').replace(/[^a-z0-9]/gi,'')||'jpg'):'jpg'};
  function cfg(){if(typeof githubLoadConfig==='function')return githubLoadConfig();try{return JSON.parse(localStorage.getItem('kapaspont_github_sync')||localStorage.getItem('horgaszpro_github_sync')||'{}')}catch(e){return {}}}
  function check(c){if(typeof githubRequireConfig==='function')return githubRequireConfig(c);const miss=[];['owner','repo','branch','token'].forEach(k=>{if(!c[k])miss.push(k)});if(miss.length)throw new Error('Hiányzó GitHub beállítás: '+miss.join(', '));}
  function root(c){return String(c.root||'kapaspont').replace(/^\/+|\/+$/g,'')||'kapaspont'}
  function fpath(c,p){return root(c)+'/'+String(p||'').replace(/^\/+/, '')}
  function apiPath(p){return String(p).split('/').map(encodeURIComponent).join('/')}
  async function gh(c,url,opts){const res=await fetch(url,{...(opts||{}),headers:{Accept:'application/vnd.github+json',Authorization:'Bearer '+c.token,'X-GitHub-Api-Version':'2022-11-28',...((opts&&opts.headers)||{})}});const txt=await res.text();let data;try{data=txt?JSON.parse(txt):null}catch(e){data={message:txt}}if(!res.ok){const er=new Error((data&&data.message)||('GitHub API hiba: '+res.status));er.status=res.status;throw er}return data}
  async function getJson(c,p){try{const d=await gh(c,'https://api.github.com/repos/'+encodeURIComponent(c.owner)+'/'+encodeURIComponent(c.repo)+'/contents/'+apiPath(p)+'?ref='+encodeURIComponent(c.branch)+'&t='+Date.now());const txt=dec(d.content||'');return txt?JSON.parse(txt):null}catch(e){return null}}
  async function put(c,p,b64,msg,mode){
    if(!b64||String(b64).length<8)throw new Error('Üres fájlt nem mentek: '+p);
    const url='https://api.github.com/repos/'+encodeURIComponent(c.owner)+'/'+encodeURIComponent(c.repo)+'/contents/'+apiPath(p);
    if(mode==='create')return gh(c,url,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:msg||'KapásPont új fájl',content:b64,branch:c.branch})});
    let sha=null;try{const old=await gh(c,url+'?ref='+encodeURIComponent(c.branch)+'&t='+Date.now());sha=old&&old.sha}catch(e){if(!String(e.message||'').includes('Not Found'))throw e}
    const body={message:msg||'KapásPont fájl frissítés',content:b64,branch:c.branch};if(sha)body.sha=sha;
    return gh(c,url,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})
  }
  function thumb(dataUrl){return new Promise(resolve=>{try{const img=new Image();img.onload=()=>{try{const max=180;let w=img.width,h=img.height;const r=Math.min(1,max/Math.max(w,h));w=Math.max(1,Math.round(w*r));h=Math.max(1,Math.round(h*r));const c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').drawImage(img,0,0,w,h);resolve(c.toDataURL('image/jpeg',0.38))}catch(e){resolve('')}};img.onerror=()=>resolve('');img.src=dataUrl}catch(e){resolve('')}})}
  async function sanitize(db,c){const out=JSON.parse(JSON.stringify(db||{}));const imgs=[];async function add(dataUrl,rel,label){if(!String(dataUrl||'').startsWith('data:image/'))return null;const p=fpath(c,rel+'.'+ext(dataUrl));imgs.push({path:p,base64:ib64(dataUrl),label});return {storage:'github',path:p,relativePath:rel+'.'+ext(dataUrl),thumb:await thumb(dataUrl),bytes:Math.round(ib64(dataUrl).length*0.75),createdAt:new Date().toISOString()}}
    for(const s of (out.sessions||[])){const base='images/sessions/'+(s.date||new Date().toISOString().slice(0,10))+'/'+slug(s.id||s.location||Date.now());let i=0;for(const ca of (s.catches||[])){i++;if(ca.photo&&String(ca.photo).startsWith('data:image/')){const r=await add(ca.photo,base+'_catch_'+pad(i),'fogás kép');if(r){ca.photo=r.thumb;ca.photoRef=r;ca.photoPath=r.path;ca.photoStorage='github'}}}let j=0;for(const ev of (s.events||[])){j++;if(ev.photo&&String(ev.photo).startsWith('data:image/')){const r=await add(ev.photo,base+'_event_'+pad(j),'esemény kép');if(r){ev.photo=r.thumb;ev.photoRef=r;ev.photoPath=r.path;ev.photoStorage='github'}}}}
    out.githubImageFiles=imgs.map(x=>({path:x.path,label:x.label}));return {db:out,imgs};}
  function storage(db,imgCount){let s='';try{s=JSON.stringify(db||{})}catch(e){}return {bytes:s.length,localBytes:s.length,fishDbBytes:0,imageBytes:0,imageCount:imgCount||0,quota:52428800,pct:Math.round((s.length/52428800)*100),sessions:(db.sessions||[]).length,locations:(db.locations||[]).length,baits:(db.baits||[]).length,gear:(db.gear||[]).length,setups:(db.setups||[]).length,mapPoints:(db.sessions||[]).reduce((a,x)=>a+(x.mapPoints||[]).length,0)}}
  function countData(d){return {sessions:(d.sessions||[]).length,locations:(d.locations||[]).length,scoutSpots:(d.scoutSpots||[]).length,catches:(d.sessions||[]).reduce((a,s)=>a+(s.catches||[]).length,0)}}
  function hasMeaningful(d){const c=countData(d||{});return c.sessions>0||c.locations>0||c.scoutSpots>0||c.catches>0}
  async function guard(c,local){
    const lc=countData(local||{});
    if(!hasMeaningful(local))throw new Error('Biztonsági védelem: nincs helyi túra/hely/fogás adat, ezért nem készítek GitHub mentést. Előbb töltsd vissza a backupot.');
    const man=await getJson(c,fpath(c,'manifest.json'));
    if(man&&man.backup){const remote=await getJson(c,man.backup);if(remote&&hasMeaningful(remote)){const rc=countData(remote);if(lc.sessions<rc.sessions||lc.catches<rc.catches){const ok=confirm('VÉDELEM: a legutóbbi GitHub backup több adatot tartalmaz, mint a telefonon lévő adat.\n\nTelefon: '+lc.sessions+' túra, '+lc.catches+' fogás\nGitHub backup: '+rc.sessions+' túra, '+rc.catches+' fogás\n\nKészítesz így is ÚJ mentésfájlt? A régi backup nem íródik felül.');if(!ok)throw new Error('Mentés megszakítva: a GitHubon több adat van.');}}}
  }
  async function sync(auto){if(running){toast('Már fut egy GitHub mentés. Várd meg, amíg befejeződik.');log('Második mentés blokkolva: az előző még fut.');return;}running=true;try{const c=cfg();check(c);if(typeof githubClearLog==='function')githubClearLog();log('Append-only GitHub mentés indul.');const local=typeof getDB==='function'?getDB():{};await guard(c,local);const pack=await sanitize(local,c);for(const im of pack.imgs){await put(c,im.path,im.base64,'KapásPont kép mentés')}const ts=stamp();const backupPath=fpath(c,'backups/full_backup_'+ts+'.json');const backup={schemaVersion:5,backupType:'github-sync-full',app:'KapásPont',appVersion:'v29.5',release:'append-only-backups',exported:new Date().toISOString(),storage:storage(pack.db,pack.imgs.length),fishImageAssets:[],...pack.db};const js=JSON.stringify(backup,null,2);if(js.length<100)throw new Error('Túl kicsi JSON mentés, nem készítek backupot.');await put(c,backupPath,enc(js),'KapásPont új időbélyeges backup','create');for(const s of (pack.db.sessions||[])){const p=fpath(c,'sessions/'+(s.date||'datum-nelkul')+'_'+slug(s.location||'hely')+'_'+slug(s.id)+'_'+ts+'.json');await put(c,p,enc(JSON.stringify({schemaVersion:2,backupType:'session',app:'KapásPont',exported:new Date().toISOString(),session:s},null,2)),'KapásPont új horgászat fájl','create')}const man={schemaVersion:3,mode:'append-only',created:new Date().toISOString(),app:'KapásPont',sessionCount:(pack.db.sessions||[]).length,locationCount:(pack.db.locations||[]).length,catchCount:(pack.db.sessions||[]).reduce((a,s)=>a+(s.catches||[]).length,0),imageCount:pack.imgs.length,backup:backupPath,latestBackup:backupPath,note:'A teljes adatmentés mindig új backups/full_backup_*.json fájlba készül. Ha az utolsó rossz, töröld azt a backup fájlt, a visszatöltés a következő használhatót keresi.'};await put(c,fpath(c,'manifest.json'),enc(JSON.stringify(man,null,2)),'KapásPont manifest frissítés');toast('Új GitHub backup kész: '+ts);log('Mentés kész. Új fájl készült: '+backupPath+'. A latest fájl nem íródott felül.');if(typeof renderStorageOverview==='function')renderStorageOverview();}catch(e){console.error(e);toast('Mentési hiba: '+e.message);log('Mentési hiba: '+e.message)}finally{running=false;}}
  function activateButtons(){document.querySelectorAll('button').forEach(b=>{const t=(b.textContent||'').toLowerCase();if(t.includes('mentés githubra most')){b.disabled=false;b.classList.remove('disabled');b.style.pointerEvents='auto';b.onclick=function(ev){ev.preventDefault();ev.stopPropagation();sync(false);};}})}
  function install(){if(typeof getDB!=='function'){setTimeout(install,250);return}window.githubScheduleAutoBackup=function(){log('Automatikus GitHub háttérmentés kihagyva. Kézi mentés használandó.');};window.githubSyncNow=function(auto){return sync(false)};try{const c=cfg();c.auto=false;localStorage.setItem('kapaspont_github_sync',JSON.stringify(c));}catch(e){}activateButtons();setInterval(activateButtons,1000);log('Append-only GitHub mentés aktív: minden mentés új backup fájl.');}
  install();
})();